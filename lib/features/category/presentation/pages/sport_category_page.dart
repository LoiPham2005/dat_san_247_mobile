import 'package:flutter/material.dart';

class SportCategoryPage extends StatelessWidget {
  const SportCategoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}